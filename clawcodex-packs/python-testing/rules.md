# Python Testing Rules

- Prefer targeted tests before broad suites.
- Run `python -m pytest -q` when pytest configuration exists.
- Do not mark complete without reporting tests run or why tests were skipped.
